import HeroStartAnimation from '../heroAnimation';

const HeroStart = () => {
  return (
    <div className="w-full h-fit flex justify-center items-center bg-white p-[5rem] max-xl:px-[1rem]">
      <HeroStartAnimation />
    </div>
  );
};

export default HeroStart;
